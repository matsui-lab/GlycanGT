# GlycanGT
GlycanGT is a graph transformer–based pre-trained model for glycan analysis. It can be used to extract embeddings, predict ambiguous glycan sequences, and fine-tune for specific downstream tasks.
